import { StyleSheet } from "react-native";

const s = StyleSheet.create({
    container: {
        alignItems: "baseline",
        flexDirection: "row",
        gap: 10
    },

    title: {
        fontSize: 42,
    },

    subtitle: {
        fontSize: 24
    }
});

export { s }